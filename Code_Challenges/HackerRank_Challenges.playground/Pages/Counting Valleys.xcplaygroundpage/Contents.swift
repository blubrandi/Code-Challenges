import UIKit

func countingValleys(n: Int, s: String) -> Int {
    
    let steps = Array(s)
    print(steps)
    var stepDict: [String: Int] = [:]

    for (_, step) in s.enumerated() {

    }
    
    return 0
}

countingValleys(n: 8, s: "UDDDUDUU")

// count consecutive occurrences of items in the array
